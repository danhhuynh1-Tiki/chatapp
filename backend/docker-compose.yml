version: "3.8"

services:
  mongodb:
    image: mongo
    container_name: chat
    environment:
      - PUID=1000
      - PGID=1000
      - MONGO_INITDB_DATABASE=chat
    volumes:
      - ./database:/data/db
    ports:
      - 27017:27017
    restart: unless-stopped